function circleArea(r)  {
    const alan = Math.PI * r * r;
    console.log(alan)
}

function circleCircumference(r) {
    const cevre = Math.PI * 2 * r;
    console.log(cevre)
}

export {
    circleArea,
    circleCircumference
}